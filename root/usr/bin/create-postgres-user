#!/bin/bash

USER=$1
DB=$2
PASSWORD=$(openssl rand -base64 32 | tr -d /=+ | cut -c -16)
createdb "$DB"
psql -c "CREATE USER $USER WITH ENCRYPTED PASSWORD '$PASSWORD'; GRANT ALL PRIVILEGES ON DATABASE $DB TO $USER;"
echo -n "$PASSWORD"
